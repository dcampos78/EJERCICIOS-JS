let animals = ['Caracol', 'Mosquito', 'Salamandra', 'Ajolote'];

function findArrayIndex(param, a) {
    for(let i = 0; i < param.length; i++){
        if (param[i] === a){
            true;
            let position = param[i].indexOf(a);
            return  position;
        }
    }
    return false;
}
function removeItem(param, a) {
    param.splice(function findArrayIndex(param,a),1);
    }     

let prueba = function removeItem(animals, Caracol);
console.log(prueba);

rehacer
